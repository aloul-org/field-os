import { SettingsPlaceholder } from "@/components/settings/SettingsPlaceholder";

export const metadata = { title: "Lead capture" };

export default function LeadCaptureSettingsPage() {
  return (
    <SettingsPlaceholder
      title="Lead capture"
      description="Embeddable website widget and inbound WhatsApp/SMS routing. Built in Phase 3."
    />
  );
}
